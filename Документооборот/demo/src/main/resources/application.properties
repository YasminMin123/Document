spring.application.name=documentobrot

# Настройки подключения к PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/document_management
spring.datasource.username=postgres
spring.datasource.password=pipokilo5525

# Hibernate настройки
spring.jpa.hibernate.ddl-auto=update
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# Настройки подключения к PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/document_management
spring.datasource.username=postgres
spring.datasource.password=your_password

# Настройки Hibernate для автоматического создания таблиц (можно изменить для продакшн-версии)
spring.jpa.hibernate.ddl-auto=update

# Настройки для использования драйвера PostgreSQL
spring.datasource.driver-class-name=org.postgresql.Driver

# Логирование SQL запросов (по желанию)
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
